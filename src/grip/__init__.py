"""grip — research package.

Submodules:
  data    — synthetic task generators with known latent ground truth.
  models  — attention mechanisms (dense, local, content-sparse, grip).
  train   — training loops, checkpointing, logging.
  eval    — metrics, ablations, scoring (never trust the trainer's self-report).
  analysis — probes, attention inspection, causal ablation utilities.
"""

__version__ = "0.0.1"
